spring:
  application:
    name: ai-orchestrator-service
server:
  port: 8083
management:
  endpoints:
    web:
      exposure:
        include: health,info
logging:
  level:
    root: INFO
    com.retail.ai.ai.orchestrator.service: DEBUG

